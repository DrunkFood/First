开发环境需同时启动四个后端服务（默认端口 8080~8083），Vite 通过 `server.proxy` 将 `/core-api`、`/ai-api`、`/file-api`、`/support-api` 转发至对应服务，请求日志写入 `logs/vite-proxy.log`；支持 `pnpm dev:local`（--mode localdev）和 `pnpm dev:test`（--mode test）切换不同 .env.* 环境变量。