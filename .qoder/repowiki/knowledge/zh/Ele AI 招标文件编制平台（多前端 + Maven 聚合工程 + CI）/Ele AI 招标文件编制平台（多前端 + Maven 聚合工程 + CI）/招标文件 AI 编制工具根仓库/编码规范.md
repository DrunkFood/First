- DB 表前缀按服务域划分：tb_(核心服务) / ai_(AI服务) / sup_(支撑中心) / file_(文件服务)，实体通过 @TableName 显式指定
- 返回结构统一使用 Result.success(data)/Result.fail(code,msg)，交互接口用 InteractionResult<T>
- 实体继承 BaseEntity 自动填充 create/modify 时间、ver、is_delete 字段
- 新依赖版本声明在父 POM <dependencyManagement>，禁止子模块重复声明版本
- 公开 API 保持 JDK 8 兼容，协议 DTO 只放 ele-ai-tender-common-interaction 模块