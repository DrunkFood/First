在根目录执行 `mvn clean install` 即可一次性构建全部子模块；若仅需某个应用，可在对应子模块目录执行 `mvn spring-boot:run` 或 `mvn package` 生成独立 jar。`ele-ai-tender-interaction` 子树因覆盖 `maven-compiler-plugin` 的 `release=8`，需确保本地 JDK 21 仍能编译 JDK 8 源码。