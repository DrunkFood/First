根级 `mvn -pl ele-ai-tender-common,ele-ai-tender-interaction -am clean install` 先安装依赖包，再单独 `mvn package -pl <module>` 构建任一子模块；前端在 `ele-ai-tender-frontend` 或 `ele-ai-tender-support-frontend` 下执行 `npm run dev`，通过 `vite.config.ts` 中 `/api` 代理转发到对应后端端口。