- 后端所有 Spring Boot 应用共享 ele-ai-tender-common 公共包，统一使用其中的 DTO、Entity、Enum、Exception、Security 等基础类型
- AI 能力通过 ele-ai-tender-interaction SPI 暴露接口，core 与 ai 模块仅依赖 SPI 而不直接耦合具体实现
- 每个后端服务模块均提供 application-dev.yml / application-test.yml / application-wuyx.yml 三套环境配置，保持命名一致