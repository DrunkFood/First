# 2026-03-11 外部 token 有效期临时调整需求整理

## 背景
- 业务系统跳转电子标编制页时依赖外部 token。
- 当前外部 token 有效期较短，联调阶段容易过期导致重复跳转与重新换 token。

## 本次需求
- 将外部 token 有效期临时放宽到 1 周，降低联调中断。
- 上线前再恢复为原有较短时效。

## 目标
- 外部 token 的 JWT 过期时间调整为 7 天。
- Redis 中外部 token 会话过期时间同步调整为 7 天。
- 外部 token 下发接口返回的 `expireIn` 与实际过期时间一致。
