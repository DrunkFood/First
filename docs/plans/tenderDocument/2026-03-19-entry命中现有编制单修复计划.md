# 2026-03-19 entry命中现有编制单修复计划

1. 在 `TenderDocumentServiceTest` 新增用例：
   - 场景：同维度无 `DRAFT`，但存在 `COMPLETED`。
   - 期望：`entry` 返回该 `COMPLETED` 的 `tenderDocumentId`，且不调用 `tenderDocumentMapper.insert`。

2. 先运行该单测，确认 RED（当前实现会创建新草稿，测试应失败）。

3. 修改 `TenderDocumentServiceImpl.enter`：
   - 保留“优先命中 DRAFT”。
   - 若无 DRAFT 且存在 latest document，则直接返回 latest document。
   - 仅在完全无记录时创建新草稿。

4. 运行 `TenderDocumentServiceTest` 全量，确认 GREEN。

5. 输出变更结论与风险点（是否影响“重新编制”流程）。
