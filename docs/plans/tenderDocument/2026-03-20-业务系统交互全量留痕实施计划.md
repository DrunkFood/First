# 2026-03-20 业务系统交互全量留痕实施计划

## 实施步骤
1. RED：补测试
- 在 `BusinessSystemRemoteClientTest` 增加断言：成功与失败调用均触发访问日志持久化。

2. GREEN：最小实现
- 在 `BusinessSystemRemoteClient` 注入可选 `AccessLogPersistenceService`。
- 在 `postForData/postWithoutData` 的成功、协议失败、网络异常分支统一落库 `sup_access_log`。
- `log_type` 固定为 `INTERACTION_OUT`，并写入业务检索键与脱敏头摘要。

3. 回归验证
- 运行 `BusinessSystemRemoteClientTest`。
- 运行 `TenderDocumentGenerationServiceTest`，确认生成/回传链路不回归。

## 风险与控制
- 风险：日志字段过长影响写入或检索。
- 控制：请求/响应摘要限长，敏感字段脱敏，业务键字段结构化存储。
