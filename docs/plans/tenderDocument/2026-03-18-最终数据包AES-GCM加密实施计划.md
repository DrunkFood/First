# 最终数据包 AES-GCM 加密实施计划

## 1. 测试先行

- 调整 `DefaultFinalPackageEncryptorTest`：
  - 不再断言 `ENC:` 前缀。
  - 增加“加密后可按 AES-GCM 解密还原”的断言。
  - 增加“默认配置项可读取”的断言（算法、密钥、后缀、水印）。

## 2. 实现改造

- 改造 `DefaultFinalPackageEncryptor`：
  - 使用 `AES/GCM/NoPadding`。
  - 使用随机 12 字节 nonce。
  - 输出二进制格式：`magic(4) + version(1) + nonce(12) + ciphertextAndTag`。
- 扩展 `TenderDocumentInteractionProperties`：
  - 增加 AES 加密算法配置。
  - 增加 AES key(base64) 配置。
- 更新 `application.yml` 默认值。

## 3. 回归验证

- 执行：
  - `mvn -Dtest=DefaultFinalPackageEncryptorTest test`
  - `mvn -Dtest=TenderDocumentGenerationServiceTest,TenderDocumentGenerationControllerTest,InteractionTenderDocumentGenerationGatewayTest,FinalPackagePayloadSerializationTest test`
  - `mvn -pl ele-tender-tender-document -am test`

## 4. 交付

- 提交本次代码与文档。
- 说明当前加密格式与密钥配置方式，提示生产环境必须覆盖默认密钥。
