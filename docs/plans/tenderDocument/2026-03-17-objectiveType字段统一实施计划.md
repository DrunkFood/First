# 2026-03-17 objectiveType 字段统一实施计划

## Plan
1. 移除后端 `jectiveType` 兼容逻辑，统一代码字段为 `objectiveType`。
2. 同步更新相关单元测试，删除旧字段兼容断言。
3. 增加数据库迁移脚本，批量修复历史 JSON 数据键名。
4. 校对并同步接口文档/联调文档。
5. 全仓复查关键字，确认无残留；执行最小化测试验证。
