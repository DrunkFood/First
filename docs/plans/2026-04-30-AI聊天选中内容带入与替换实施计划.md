# AI聊天选中内容带入与替换 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用户在需求页面选中文字后自动带入AI聊天，AI回复后可一键替换选中原文

**Architecture:** 从底层到顶层依次修改：类型定义 → 编辑器选中事件 → 聊天面板引用块+发送逻辑+替换操作栏 → 侧边栏透传 → 页面组装与替换逻辑

**Tech Stack:** Vue 3 + TipTap + Element Plus + TypeScript

---

## File Structure

| 操作 | 文件 | 职责 |
|------|------|------|
| Modify | `ele-ai-tender-frontend/src/types/ai.ts` | AiChatMessage 新增 selectedText 字段 |
| Modify | `ele-ai-tender-frontend/src/components/editor/WysiwygEditor.vue` | 监听 TipTap selectionUpdate，emit selection-change |
| Modify | `ele-ai-tender-frontend/src/components/ai/AiChatPanel.vue` | 引用块UI、context区分、用户消息引用展示、替换操作栏、replace emit |
| Modify | `ele-ai-tender-frontend/src/components/ai/AiAssistantSidebar.vue` | 透传 selectedText prop 和 replace emit |
| Modify | `ele-ai-tender-frontend/src/views/requirement/RequirementGenerate.vue` | selectedText ref、事件串联、替换逻辑 |

---

### Task 1: AiChatMessage 类型扩展

**Files:**
- Modify: `ele-ai-tender-frontend/src/types/ai.ts`

- [ ] **Step 1: 在 AiChatMessage 接口中新增 selectedText 字段**

在 `ele-ai-tender-frontend/src/types/ai.ts` 的 `AiChatMessage` 接口中新增：

```typescript
export interface AiChatMessage {
  role: 'user' | 'assistant'
  content: string
  timestamp: number
  error?: boolean
  /** 消息唯一标识，用于反馈关联 */
  uid?: string
  /** 是否为欢迎语（不显示反馈按钮、不作为对话历史发送） */
  isGreeting?: boolean
  /** 用户选中的原文，用于后续替换定位。仅用户消息携带 */
  selectedText?: string
}
```

- [ ] **Step 2: Commit**

```bash
git add ele-ai-tender-frontend/src/types/ai.ts
git commit -m "feat(ai-chat): add selectedText field to AiChatMessage type"
```

---

### Task 2: WysiwygEditor 选中事件

**Files:**
- Modify: `ele-ai-tender-frontend/src/components/editor/WysiwygEditor.vue`

- [ ] **Step 1: 新增 selection-change emit 和防抖监听**

在 `<script setup>` 中：

1. 新增 emit 声明（在 `const modelValue = defineModel<string>({ default: '' })` 之后）：

```typescript
const emit = defineEmits<{
  'selection-change': [text: string]
}>()
```

2. 新增防抖选中监听变量和逻辑（在 `let flushTimer` 之前）：

```typescript
let selectionTimer: ReturnType<typeof setTimeout> | null = null
```

3. 在 `useEditor` 初始化之后、`scheduleFlush` 函数之前，添加监听逻辑：

```typescript
// 监听文本选中变化，防抖 300ms
if (editor.value) {
  editor.value.on('selectionUpdate', ({ editor: ed }) => {
    if (selectionTimer) clearTimeout(selectionTimer)
    selectionTimer = setTimeout(() => {
      const { from, to, empty } = ed.state.selection
      if (empty) {
        emit('selection-change', '')
        return
      }
      const text = ed.state.doc.textBetween(from, to, '\n')
      emit('selection-change', text.length >= 5 ? text : '')
    }, 300)
  })
}
```

4. 在 `onBeforeUnmount` 中清理 timer：

```typescript
onBeforeUnmount(() => {
  if (selectionTimer) {
    clearTimeout(selectionTimer)
    selectionTimer = null
  }
  if (flushTimer) {
    clearTimeout(flushTimer)
    flushTimer = null
  }
  editor.value?.destroy()
})
```

- [ ] **Step 2: 验证编译通过**

Run: `cd ele-ai-tender-frontend && npx vue-tsc --noEmit 2>&1 | head -20`
Expected: 无类型错误（或有与本次改动无关的既有错误）

- [ ] **Step 3: Commit**

```bash
git add ele-ai-tender-frontend/src/components/editor/WysiwygEditor.vue
git commit -m "feat(editor): add selection-change emit with debounce for text selection tracking"
```

---

### Task 3: AiChatPanel — 引用块UI与发送逻辑

**Files:**
- Modify: `ele-ai-tender-frontend/src/components/ai/AiChatPanel.vue`

这是改动最大的 Task，分为多个 Step。

- [ ] **Step 1: 新增 selectedText prop 和相关 emits**

在 `<script setup>` 中：

1. 扩展 props（替换原有 props 定义）：

```typescript
const props = defineProps<{
  context?: string
  projectId?: number
  requirementId?: number
  showClose?: boolean
  greeting?: string
  selectedText?: string
}>()
```

2. 扩展 emits（替换原有 emit 定义）：

```typescript
const emit = defineEmits<{
  close: []
  feedback: [type: 'like' | 'dislike', msg: AiChatMessage]
  message: [content: string]
  replace: [payload: { selectedText: string; replacement: string }]
}>()
```

- [ ] **Step 2: 引用块模板 — 输入框上方**

在 `<template>` 的 `<!-- 底部输入区 -->` 部分，`<div class="chat-input">` 内部、快捷操作插槽之后、`<div class="chat-input-row">` 之前，插入引用块：

```html
<!-- 选中内容引用块 -->
<div v-if="selectedText" class="selection-quote">
  <div class="quote-header">
    <span class="quote-label">选中的内容</span>
    <button class="quote-close" @click="clearSelection" title="清除">&times;</button>
  </div>
  <div class="quote-content">{{ selectedText }}</div>
</div>
```

注意：这里用 `selectedText` 直接引用 props，因为不需要本地修改。但发送后需要清除，所以需要一个本地的清除方法。实际上 selectedText 来自父组件，清除需要通知父组件。

改为：用 computed 引用 props，emit 一个清除事件。或者更简单地——既然 selectedText 是父组件传入的，清除由父组件处理，但引用块的关闭按钮需要通知父组件。

修正方案：新增 `update:selectedText` emit，让引用块的关闭按钮通知父组件清除：

修改 emits 为：

```typescript
const emit = defineEmits<{
  close: []
  feedback: [type: 'like' | 'dislike', msg: AiChatMessage]
  message: [content: string]
  replace: [payload: { selectedText: string; replacement: string }]
  'update:selectedText': [value: string]
}>()
```

清除方法：

```typescript
function clearSelection() {
  emit('update:selectedText', '')
}
```

模板中引用块改为：

```html
<!-- 选中内容引用块 -->
<div v-if="props.selectedText" class="selection-quote">
  <div class="quote-header">
    <span class="quote-label">选中的内容</span>
    <button class="quote-close" @click="clearSelection" title="清除">&times;</button>
  </div>
  <div class="quote-content">{{ props.selectedText }}</div>
</div>
```

- [ ] **Step 3: 用户消息气泡中的引用展示**

在消息列表中，用户消息的 `<template v-else>` 部分（即 `msg.role === 'user'`），替换为：

```html
<template v-else>
  <div class="message-content">
    <div v-if="msg.selectedText" class="user-quote">
      {{ msg.selectedText }}
    </div>
    <div>{{ msg.content }}</div>
  </div>
</template>
```

- [ ] **Step 4: 替换操作栏**

在 AI 消息气泡的反馈按钮区域后面（`<!-- AI消息反馈按钮 -->` 的 `</div>` 之后），添加替换操作栏：

```html
<!-- 替换操作栏（仅关联了 selectedText 的 AI 回复显示） -->
<div v-if="msg.role === 'assistant' && msg.content && !sending && !msg.isGreeting && !msg.error && getSelectedTextForAiMsg(index)" class="replace-actions">
  <el-button size="small" type="primary" @click="handleReplace(index, msg)">
    应用替换
  </el-button>
  <el-button size="small" @click="dismissReplace(index)">
    取消
  </el-button>
</div>
```

需要新增的状态和方法（在 `<script setup>` 中）：

```typescript
/** 记录已取消替换的消息索引 */
const dismissedReplace = ref<Set<number>>(new Set())

/** 获取 AI 回复消息对应的用户选中原文 */
function getSelectedTextForAiMsg(aiMsgIndex: number): string | undefined {
  if (dismissedReplace.value.has(aiMsgIndex)) return undefined
  // AI 回复的前一条是用户消息
  if (aiMsgIndex <= 0) return undefined
  const userMsg = messages.value[aiMsgIndex - 1]
  if (userMsg?.role !== 'user') return undefined
  return userMsg.selectedText
}

/** 应用替换 */
function handleReplace(_index: number, msg: AiChatMessage) {
  const selectedText = getSelectedTextForAiMsg(_index)
  if (!selectedText) return
  emit('replace', { selectedText, replacement: msg.content })
  dismissedReplace.value.add(_index)
}

/** 取消替换 */
function dismissReplace(index: number) {
  dismissedReplace.value.add(index)
}
```

- [ ] **Step 5: 修改发送逻辑 — 区分 context**

修改 `handleSend` 函数，在构建 SSE 请求体时：

将原来的 `context: props.context` 改为 `context: props.selectedText || ''`：

```typescript
closeSSE = createSSEConnection(
  aiApi.chatUrl,
  {
    message: text,
    context: props.selectedText || '',    // 用户输入：有选中用选中，否则空
    projectId: props.projectId,
    requirementId: props.requirementId,
    conversationId,
    history,
  },
  // ... 回调不变
)
```

同时在用户消息中携带 selectedText：

```typescript
const userMsg: AiChatMessage = {
  role: 'user',
  content: text,
  timestamp: Date.now(),
  uid: generateUid('user', Date.now(), text),
  selectedText: props.selectedText || undefined,  // 记录选中内容
}
```

发送后清除选中内容：

```typescript
// 在 inputText.value = '' 之后添加
if (props.selectedText) {
  emit('update:selectedText', '')
}
```

- [ ] **Step 6: 修改快捷操作 — 区分 context**

修改 `sendQuickAction` 函数，使快捷操作使用不同的 context：

```typescript
/** 快捷操作：设置输入内容并发送 */
function sendQuickAction(text: string) {
  if (sending.value || !text.trim()) return
  inputText.value = text
  // 快捷操作需要走独立发送逻辑，使用 selectedText || context
  sendWithQuickActionContext(text)
}

/** 快捷操作发送（context fallback 到完整内容） */
function sendWithQuickActionContext(text: string) {
  if (!text.trim() || sending.value) return

  const history = messages.value
    .filter(msg => msg.content && !msg.error && !msg.isGreeting)
    .map(msg => ({ role: msg.role, content: msg.content }))
    .slice(-10)

  const userMsg: AiChatMessage = {
    role: 'user',
    content: text,
    timestamp: Date.now(),
    uid: generateUid('user', Date.now(), text),
    selectedText: props.selectedText || undefined,
  }
  messages.value.push(userMsg)
  inputText.value = ''
  if (props.selectedText) {
    emit('update:selectedText', '')
  }
  scrollToBottom()

  const aiMsgTimestamp = Date.now()
  const aiMsg: AiChatMessage = { role: 'assistant', content: '', timestamp: aiMsgTimestamp, uid: generateUid('assistant', aiMsgTimestamp) }
  messages.value.push(aiMsg)
  const aiIndex = messages.value.length - 1

  sending.value = true
  emit('message', text)

  closeSSE = createSSEConnection(
    aiApi.chatUrl,
    {
      message: text,
      context: props.selectedText || props.context || '',   // 快捷操作：有选中用选中，否则用全文
      projectId: props.projectId,
      requirementId: props.requirementId,
      conversationId,
      history,
    },
    (data: string) => {
      const current = messages.value[aiIndex]!
      const newContent = current.content + data
      messages.value.splice(aiIndex, 1, {
        role: current.role,
        content: newContent,
        timestamp: current.timestamp,
        uid: generateUid('assistant', current.timestamp, newContent),
      })
      scrollToBottom()
    },
    () => {
      const current = messages.value[aiIndex]
      if (current) {
        messages.value.splice(aiIndex, 1, {
          role: current.role,
          content: current.content || '',
          timestamp: current.timestamp,
          uid: generateUid('assistant', current.timestamp, current.content),
          error: true,
        })
      }
      sending.value = false
      closeSSE = null
    },
    () => {
      sending.value = false
      closeSSE = null
    },
  )

  scrollToBottom()
}
```

- [ ] **Step 7: 引用块和替换操作栏的样式**

在 `<style>` 中添加：

```scss
// ========================================
// 选中内容引用块
// ========================================
.selection-quote {
  background: var(--app-bg-elevated);
  border: 1px solid var(--app-border-light);
  border-left: 3px solid var(--app-brand-color);
  border-radius: 4px;
  padding: 8px 10px;
  margin-bottom: 4px;
}

.quote-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: 4px;
}

.quote-label {
  font-size: 11px;
  color: var(--app-text-tertiary);
  font-weight: 500;
}

.quote-close {
  background: none;
  border: none;
  color: var(--app-text-tertiary);
  cursor: pointer;
  font-size: 16px;
  line-height: 1;
  padding: 0 2px;
}

.quote-close:hover {
  color: var(--app-text-secondary);
}

.quote-content {
  font-size: 12px;
  color: var(--app-text-secondary);
  line-height: 1.5;
  max-height: 4.5em;
  overflow: hidden;
  display: -webkit-box;
  -webkit-line-clamp: 3;
  -webkit-box-orient: vertical;
  word-break: break-all;
}

// ========================================
// 用户消息中的引用
// ========================================
.user-quote {
  border-left: 3px solid rgba(255, 255, 255, 0.4);
  padding-left: 8px;
  margin-bottom: 6px;
  font-size: 13px;
  opacity: 0.8;
  white-space: pre-wrap;
  word-break: break-all;
}

// ========================================
// 替换操作栏
// ========================================
.replace-actions {
  display: flex;
  gap: 8px;
  margin-top: 8px;
  justify-content: flex-end;
}
```

- [ ] **Step 8: 验证编译通过**

Run: `cd ele-ai-tender-frontend && npx vue-tsc --noEmit 2>&1 | head -20`
Expected: 无类型错误

- [ ] **Step 9: Commit**

```bash
git add ele-ai-tender-frontend/src/components/ai/AiChatPanel.vue
git commit -m "feat(ai-chat): add selection quote block, context split, and replace action bar"
```

---

### Task 4: AiAssistantSidebar 透传

**Files:**
- Modify: `ele-ai-tender-frontend/src/components/ai/AiAssistantSidebar.vue`

- [ ] **Step 1: 新增 selectedText prop 和 replace/update:selectedText emit**

1. 扩展 props：

```typescript
const props = defineProps<{
  visible: boolean
  messages: AiChatMessage[]
  greeting?: string
  context?: string
  projectId?: number
  requirementId?: number
  selectedText?: string
}>()
```

2. 扩展 emits：

```typescript
const emit = defineEmits<{
  'update:visible': [value: boolean]
  'update:messages': [value: AiChatMessage[]]
  feedback: [type: 'like' | 'dislike', msg: AiChatMessage]
  message: [content: string]
  replace: [payload: { selectedText: string; replacement: string }]
  'update:selectedText': [value: string]
}>()
```

3. 更新模板中 AiChatPanel 的使用（替换原有）：

```html
<AiChatPanel
  ref="chatPanelRef"
  show-close
  :greeting="greeting"
  :context="context"
  :project-id="projectId"
  :requirement-id="requirementId"
  :selected-text="selectedText"
  v-model:messages="messages"
  @close="emit('update:visible', false)"
  @feedback="(type, msg) => emit('feedback', type, msg)"
  @message="(content) => emit('message', content)"
  @replace="(payload) => emit('replace', payload)"
  @update:selected-text="(val) => emit('update:selectedText', val)"
>
  <template #quick-actions>
    <slot name="quick-actions" />
  </template>
</AiChatPanel>
```

- [ ] **Step 2: 验证编译通过**

Run: `cd ele-ai-tender-frontend && npx vue-tsc --noEmit 2>&1 | head -20`
Expected: 无类型错误

- [ ] **Step 3: Commit**

```bash
git add ele-ai-tender-frontend/src/components/ai/AiAssistantSidebar.vue
git commit -m "feat(ai-sidebar): passthrough selectedText prop and replace emit"
```

---

### Task 5: RequirementGenerate 组装与替换逻辑

**Files:**
- Modify: `ele-ai-tender-frontend/src/views/requirement/RequirementGenerate.vue`

- [ ] **Step 1: 新增 selectedText ref**

在 `const chatMessages = ref<AiChatMessage[]>([])` 之后添加：

```typescript
const selectedText = ref('')
```

- [ ] **Step 2: WysiwygEditor 添加 selection-change 监听**

模板中 WysiwygEditor 的使用修改为：

```html
<WysiwygEditor
  v-model="content"
  :readonly="isRequirementCompleted"
  :highlights="detectionIssues"
  class="content-editor"
  @selection-change="handleSelectionChange"
/>
```

在 `<script setup>` 中添加处理函数：

```typescript
function handleSelectionChange(text: string) {
  selectedText.value = text
}
```

- [ ] **Step 3: AiAssistantSidebar 添加 selectedText 和 replace 事件**

模板中 AiAssistantSidebar 的使用修改为：

```html
<AiAssistantSidebar
  ref="aiSidebarRef"
  v-if="!isRequirementCompleted"
  v-model:visible="chatVisible"
  v-model:messages="chatMessages"
  greeting="您好！我是您的AI助手，可以帮助您修改业务需求内容。请选择或输入需要修改的内容，我会为您提供修改建议。"
  :context="content"
  :requirement-id="requirementId"
  :selected-text="selectedText"
  @feedback="handleChatFeedback"
  @message="handleAiMessage"
  @replace="handleReplace"
  @update:selected-text="selectedText = $event"
>
  <template #quick-actions>
    <div class="quick-actions">
      <button class="quick-action-btn" @click="sendQuickAction('修改项目概况')">修改项目概况</button>
      <button class="quick-action-btn" @click="sendQuickAction('修改项目目标')">修改项目目标</button>
      <button class="quick-action-btn" @click="sendQuickAction('修改技术要求')">修改技术要求</button>
    </div>
  </template>
</AiAssistantSidebar>
```

- [ ] **Step 4: 替换逻辑**

在 `<script setup>` 中添加：

```typescript
/** 处理AI回复替换选中原文 */
function handleReplace(payload: { selectedText: string; replacement: string }) {
  const { selectedText: original, replacement } = payload
  const index = content.value.indexOf(original)

  if (index === -1) {
    ElMessage.warning('原文已被修改，请手动替换')
    return
  }

  // 检查是否有多处匹配
  const secondIndex = content.value.indexOf(original, index + 1)
  if (secondIndex !== -1) {
    ElMessage.warning('存在多处相同内容，已替换第一处')
  }

  content.value = content.value.substring(0, index) + replacement + content.value.substring(index + original.length)
  ElMessage.success('替换成功')
}
```

同时修改 `handleAiMessage`，对于带选中内容的消息不再追加到末尾：

```typescript
function handleAiMessage(msg: string) {
  if (!msg.trim()) return
  // 带选中内容的消息走替换流程，不追加到末尾
  if (!selectedText.value) {
    content.value += '\n\n' + msg
    ElMessage.info('AI建议已追加到内容末尾')
  }
}
```

- [ ] **Step 5: 验证编译通过**

Run: `cd ele-ai-tender-frontend && npx vue-tsc --noEmit 2>&1 | head -20`
Expected: 无类型错误

- [ ] **Step 6: 启动开发服务器，手动验证**

Run: `cd ele-ai-tender-frontend && npm run dev`

验证步骤：
1. 打开需求生成页面
2. 选中编辑器中的一段文字（≥5字符）→ 聊天面板输入框上方应出现引用块
3. 点击引用块 ✕ → 引用块消失
4. 再次选中文字 → 引用块出现 → 输入消息发送 → 引用块消失，用户气泡显示引用
5. AI回复后 → 回复气泡底部显示"应用替换"/"取消"按钮
6. 点击"应用替换" → 编辑器中选中原文被替换
7. 使用快捷操作按钮 → 不选中文字时发送，context 应为完整内容
8. 选中文字后使用快捷操作 → context 应为选中内容

- [ ] **Step 7: Commit**

```bash
git add ele-ai-tender-frontend/src/views/requirement/RequirementGenerate.vue
git commit -m "feat(requirement): wire selection tracking and AI reply replacement"
```

---

## Self-Review

**1. Spec coverage:**

| 设计要求 | 对应 Task |
|----------|----------|
| AiChatMessage 新增 selectedText | Task 1 |
| TipTap selectionUpdate + 300ms防抖 + ≥5字符 | Task 2 |
| 引用块UI（输入框上方、3行截断、✕关闭） | Task 3 |
| 用户消息气泡引用展示 | Task 3 |
| context 区分（用户输入 vs 快捷操作） | Task 3 |
| 替换操作栏（应用替换/取消） | Task 3 |
| replace emit | Task 3 |
| Sidebar 透传 | Task 4 |
| 替换逻辑（唯一/多处/未找到） | Task 5 |
| 发送后清除引用块 | Task 3 |
| 边界：<5字符不触发 | Task 2 |

**2. Placeholder scan:** 无 TBD/TODO，所有步骤含完整代码。

**3. Type consistency:** `selectedText` 在所有层级均为 `string` 类型；`replace` emit payload 统一为 `{ selectedText: string; replacement: string }`。
