# 评审项模板自定义 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在模板中新增 `review_config` JSON 配置，控制评审类型的启用/禁用和评审标准的生成/占位，贯穿支撑中心配置→项目快照→AI生成→文档集成→前端展示全链路。

**Architecture:** 在 `sup_template` 和 `tb_project_template` 新增 `review_config` JSON 字段，支撑中心模板管理页面配置该字段，项目绑定时快照，AI生成/结果同步/文档集成/前端评审项页面均读取该配置进行差异化处理。

**Tech Stack:** Spring Boot 3.2.2 / MyBatis-Plus 3.5.5 / Vue 3 + TypeScript + Element Plus

---

## Task 1: 数据库新增 review_config 字段

**Files:**
- Modify: `ele-ai-tender-system/ele-ai-tender-support/sql/init.sql` (sup_template 建表)
- Modify: `ele-ai-tender-system/ele-ai-tender-core/sql/init.sql` (tb_project_template 建表)

- [ ] **Step 1: 在 sup_template 建表SQL中新增 review_config 字段**

在 `ele-ai-tender-system/ele-ai-tender-support/sql/init.sql` 的 `sup_template` 建表语句中，`structure_definition` 字段之后新增：

```sql
`review_config`         JSON         DEFAULT NULL COMMENT '评审项配置JSON',
```

- [ ] **Step 2: 在 tb_project_template 建表SQL中新增 review_config 字段**

在 `ele-ai-tender-system/ele-ai-tender-core/sql/init.sql` 的 `tb_project_template` 建表语句中，`structure_definition` 字段之后新增：

```sql
`review_config`         JSON         DEFAULT NULL COMMENT '评审项配置JSON(快照)',
```

- [ ] **Step 3: 对已有数据库执行 ALTER TABLE**

```sql
ALTER TABLE sup_template ADD COLUMN review_config JSON DEFAULT NULL COMMENT '评审项配置JSON' AFTER structure_definition;
ALTER TABLE tb_project_template ADD COLUMN review_config JSON DEFAULT NULL COMMENT '评审项配置JSON(快照)' AFTER structure_definition;
```

- [ ] **Step 4: 验证字段已添加**

Run: `rtk mysql -h 10.11.20.50 -P 15005 -u root -p ele_ai_tender -e "DESCRIBE sup_template;" 2>/dev/null | grep review_config`
Expected: 显示 review_config 字段行

- [ ] **Step 5: Commit**

```bash
rtk git add ele-ai-tender-system/ele-ai-tender-support/sql/init.sql ele-ai-tender-system/ele-ai-tender-core/sql/init.sql
rtk git commit -m "feat(review): 新增sup_template和tb_project_template的review_config字段"
```

---

## Task 2: 后端实体类新增 reviewConfig 字段

**Files:**
- Modify: `ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/entity/support/SupTemplate.java`
- Modify: `ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/entity/core/TbProjectTemplate.java`
- Create: `ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/dto/ReviewConfig.java`
- Create: `ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/dto/ReviewTypeConfig.java`

- [ ] **Step 1: 创建 ReviewConfig DTO**

创建 `ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/dto/ReviewConfig.java`:

```java
package com.jy.eleaitender.common.dto;

import lombok.Data;
import java.util.List;

/**
 * 评审项配置 — 存储在模板的 review_config JSON 字段中
 */
@Data
public class ReviewConfig {

    /** 各评审类型配置列表 */
    private List<ReviewTypeConfig> reviewTypes;

    /**
     * 构建默认配置：4种类型全部启用+生成评审标准
     */
    public static ReviewConfig defaultConfig() {
        ReviewConfig config = new ReviewConfig();
        config.setReviewTypes(List.of(
                ReviewTypeConfig.of("COMPLIANCE", true, true),
                ReviewTypeConfig.of("TECHNICAL", true, true),
                ReviewTypeConfig.of("CREDIT", true, true),
                ReviewTypeConfig.of("COMMERCIAL", true, true)
        ));
        return config;
    }

    /**
     * 判断指定评审类型是否启用
     */
    public boolean isEnabled(String reviewType) {
        return reviewTypes != null && reviewTypes.stream()
                .anyMatch(t -> reviewType.equals(t.getReviewType()) && t.isEnabled());
    }

    /**
     * 判断指定评审类型是否需要生成评审标准
     */
    public boolean isGenerateStandard(String reviewType) {
        return reviewTypes != null && reviewTypes.stream()
                .filter(t -> reviewType.equals(t.getReviewType()))
                .findFirst()
                .map(ReviewTypeConfig::isGenerateStandard)
                .orElse(true);
    }

    /**
     * 获取所有启用的评审类型
     */
    public List<ReviewTypeConfig> getEnabledTypes() {
        if (reviewTypes == null) return List.of();
        return reviewTypes.stream().filter(ReviewTypeConfig::isEnabled).toList();
    }
}
```

- [ ] **Step 2: 创建 ReviewTypeConfig DTO**

创建 `ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/dto/ReviewTypeConfig.java`:

```java
package com.jy.eleaitender.common.dto;

import lombok.Data;

/**
 * 单个评审类型的配置项
 */
@Data
public class ReviewTypeConfig {

    /** 评审类型枚举: COMPLIANCE / TECHNICAL / CREDIT / COMMERCIAL */
    private String reviewType;

    /** 是否启用 */
    private boolean enabled;

    /** 是否生成评审标准（false时item_name填充"详见评审文件"） */
    private boolean generateStandard;

    public static ReviewTypeConfig of(String reviewType, boolean enabled, boolean generateStandard) {
        ReviewTypeConfig config = new ReviewTypeConfig();
        config.setReviewType(reviewType);
        config.setEnabled(enabled);
        config.setGenerateStandard(generateStandard);
        return config;
    }
}
```

- [ ] **Step 3: SupTemplate 新增 reviewConfig 字段**

在 `SupTemplate.java` 的 `structureDefinition` 字段之后新增：

```java
@Schema(description = "评审项配置JSON")
private String reviewConfig;
```

- [ ] **Step 4: TbProjectTemplate 新增 reviewConfig 字段**

在 `TbProjectTemplate.java` 的 `structureDefinition` 字段之后新增：

```java
@Schema(description = "评审项配置JSON(快照)")
private String reviewConfig;
```

- [ ] **Step 5: install common 模块**

Run: `cd D:/Archives/IDEAProjects/tender-document-tool/ele-ai-tender-system && rtk mvn install -pl ele-ai-tender-common -am -Dmaven.test.skip=true`
Expected: BUILD SUCCESS

- [ ] **Step 6: Commit**

```bash
rtk git add ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/dto/ReviewConfig.java ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/dto/ReviewTypeConfig.java ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/entity/support/SupTemplate.java ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/entity/core/TbProjectTemplate.java
rtk git commit -m "feat(review): 新增ReviewConfig/ReviewTypeConfig DTO，SupTemplate和TbProjectTemplate增加reviewConfig字段"
```

---

## Task 3: 项目模板快照逻辑 — 复制 reviewConfig

**Files:**
- Modify: `ele-ai-tender-system/ele-ai-tender-core/src/main/java/com/jy/eleaitender/core/service/impl/ProjectTemplateServiceImpl.java`

- [ ] **Step 1: 在 bindTemplate 方法中复制 reviewConfig**

在 `ProjectTemplateServiceImpl.java` 的 `bindTemplate` 方法中，`pt.setStructureDefinition(...)` 之后新增一行：

```java
pt.setReviewConfig(supTemplate.getReviewConfig());         // 评审项配置JSON(快照)
```

- [ ] **Step 2: 验证编译通过**

Run: `cd D:/Archives/IDEAProjects/tender-document-tool/ele-ai-tender-system && rtk mvn compile -pl ele-ai-tender-core -am -Dmaven.test.skip=true`
Expected: BUILD SUCCESS

- [ ] **Step 3: Commit**

```bash
rtk git add ele-ai-tender-system/ele-ai-tender-core/src/main/java/com/jy/eleaitender/core/service/impl/ProjectTemplateServiceImpl.java
rtk git commit -m "feat(review): 项目模板快照时复制reviewConfig字段"
```

---

## Task 4: AI 生成逻辑 — 根据 reviewConfig 动态构建 Prompt

**Files:**
- Modify: `ele-ai-tender-system/ele-ai-tender-ai/src/main/java/com/jy/eleaitender/ai/processor/prompt/PromptTemplates.java`
- Modify: `ele-ai-tender-system/ele-ai-tender-ai/src/main/java/com/jy/eleaitender/ai/processor/prompt/PromptBuilder.java`
- Modify: `ele-ai-tender-system/ele-ai-tender-ai/src/main/java/com/jy/eleaitender/ai/processor/generator/ReviewItemGenerator.java`
- Modify: `ele-ai-tender-system/ele-ai-tender-core/src/main/java/com/jy/eleaitender/core/service/impl/ReviewItemServiceImpl.java`

- [ ] **Step 1: PromptTemplates 新增带配置的 User Prompt 模板**

在 `PromptTemplates.java` 中新增：

```java
/**
 * 评审项生成 - User Prompt 模板（带评审类型配置）
 * 参数: projectName, projectType, projectCategory, budget, requirementContent, reviewMethod, enabledTypes
 */
public static final String REVIEW_ITEM_GENERATE_USER_WITH_CONFIG = """
        请根据以下项目信息和需求内容生成评审标准：

        项目名称：%s
        项目类型：%s
        项目类别：%s
        项目预算：%s元

        业务需求内容：
        %s

        评审方式：%s

        评审类型配置：
        %s

        请只生成以上启用的评审类型，不要生成未列出的类型。
        请确保总分值为100分，合理分配各评审项的分值。
        """;
```

- [ ] **Step 2: PromptBuilder 新增构建方法**

在 `PromptBuilder.java` 中新增方法：

```java
/**
 * 构建评审项生成的 User Prompt（带评审类型配置）
 */
public static String buildReviewItemGenerate(String projectName, String projectType,
                                              String projectCategory, String budget,
                                              String requirementContent, String reviewMethod,
                                              String enabledTypes) {
    return String.format(PromptTemplates.REVIEW_ITEM_GENERATE_USER_WITH_CONFIG,
            projectName, projectType, projectCategory, budget,
            requirementContent, reviewMethod, enabledTypes);
}
```

- [ ] **Step 3: ReviewItemServiceImpl.submitGenerate 传递 reviewConfig**

修改 `ReviewItemServiceImpl.java` 的 `submitGenerate` 方法，在 `params.put("requirementContent", ...)` 之后新增：

```java
// 读取项目模板的评审项配置
TbProjectTemplate projectTemplate = projectTemplateService.getByProjectId(projectId);
if (projectTemplate != null && projectTemplate.getReviewConfig() != null) {
    params.put("reviewConfig", projectTemplate.getReviewConfig());
}
```

需在类中注入 `IProjectTemplateService`（如未注入）：

```java
@Autowired
private IProjectTemplateService projectTemplateService;
```

- [ ] **Step 4: ReviewItemGenerator.generate 解析 reviewConfig 并动态构建 Prompt**

修改 `ReviewItemGenerator.java` 的 `generate` 方法，替换原有 Prompt 构建逻辑：

```java
public String generate(AiTask task) {
    log.info("开始评审项生成: taskId={}", task.getId());

    Map<String, Object> params = resultParser.parseParams(task.getRequestParams());

    String reviewConfigJson = getString(params, "reviewConfig");
    String enabledTypes;

    if (StringUtils.hasText(reviewConfigJson)) {
        // 有配置：只生成启用的、需生成标准的类型
        ReviewConfig config = ReviewConfig.fromJson(reviewConfigJson);
        enabledTypes = config.getEnabledTypes().stream()
                .filter(ReviewTypeConfig::isGenerateStandard)
                .map(t -> {
                    String label = switch (t.getReviewType()) {
                        case "COMPLIANCE" -> "符合性审查";
                        case "TECHNICAL" -> "技术标评审";
                        case "CREDIT" -> "资信标评审";
                        case "COMMERCIAL" -> "商务评审";
                        default -> t.getReviewType();
                    };
                    return label;
                })
                .collect(Collectors.joining("、"));

        if (!StringUtils.hasText(enabledTypes)) {
            log.info("无启用的且需生成评审标准的类型，跳过AI调用: taskId={}", task.getId());
            return "{\"reviewItems\":[]}";
        }
    } else {
        enabledTypes = "符合性审查、技术标评审、资信标评审、商务评审";
    }

    String userPrompt = PromptBuilder.buildReviewItemGenerate(
            getString(params, "projectName"),
            getString(params, "projectType"),
            getString(params, "projectCategory"),
            getString(params, "budget"),
            getString(params, "requirementContent"),
            getString(params, "reviewMethod"),
            enabledTypes
    );

    // 路由到合适的模型
    ChatClient client = modelRouter.route(AiTaskType.REVIEW_ITEM_GENERATE);

    // 同步调用并记录响应
    String aiOutput = aiCallRecorder.callAndRecord(client, PromptTemplates.REVIEW_ITEM_GENERATE,
            userPrompt, "GENERATION", task.getId(), task.getCreateId(), task.getFileIdList());

    // 提取JSON内容
    String jsonResult = resultParser.extractJson(aiOutput);

    // 验证结果包含reviewItems字段
    if (!resultParser.validateJsonField(jsonResult, "reviewItems")) {
        log.warn("评审项生成结果缺少reviewItems字段, taskId={}", task.getId());
        throw new AiErrorContentException("AI输出格式异常，请重试", jsonResult);
    }

    log.info("评审项生成完成: taskId={}", task.getId());
    return jsonResult;
}
```

需在 ReviewItemGenerator 顶部新增 import：

```java
import com.jy.eleaitender.common.dto.ReviewConfig;
import com.jy.eleaitender.common.dto.ReviewTypeConfig;
import org.springframework.util.StringUtils;
import java.util.stream.Collectors;
```

- [ ] **Step 5: ReviewConfig 添加 fromJson 静态方法**

在 `ReviewConfig.java` 中新增：

```java
private static final com.fasterxml.jackson.databind.ObjectMapper objectMapper =
        new com.fasterxml.jackson.databind.ObjectMapper();

public static ReviewConfig fromJson(String json) {
    try {
        return objectMapper.readValue(json, ReviewConfig.class);
    } catch (Exception e) {
        return defaultConfig();
    }
}
```

- [ ] **Step 6: 验证编译通过**

Run: `cd D:/Archives/IDEAProjects/tender-document-tool/ele-ai-tender-system && rtk mvn compile -pl ele-ai-tender-ai -am -Dmaven.test.skip=true`
Expected: BUILD SUCCESS

- [ ] **Step 7: Commit**

```bash
rtk git add ele-ai-tender-system/ele-ai-tender-ai/src/main/java/com/jy/eleaitender/ai/processor/prompt/PromptTemplates.java ele-ai-tender-system/ele-ai-tender-ai/src/main/java/com/jy/eleaitender/ai/processor/prompt/PromptBuilder.java ele-ai-tender-system/ele-ai-tender-ai/src/main/java/com/jy/eleaitender/ai/processor/generator/ReviewItemGenerator.java ele-ai-tender-system/ele-ai-tender-core/src/main/java/com/jy/eleaitender/core/service/impl/ReviewItemServiceImpl.java ele-ai-tender-system/ele-ai-tender-common/src/main/java/com/jy/eleaitender/common/dto/ReviewConfig.java
rtk git commit -m "feat(review): AI生成评审项时根据reviewConfig动态构建Prompt"
```

---

## Task 5: AI 结果同步 — 处理占位节点

**Files:**
- Modify: `ele-ai-tender-system/ele-ai-tender-core/src/main/java/com/jy/eleaitender/core/scheduler/AiTaskResultSyncHandler.java`

- [ ] **Step 1: syncReviewItems 方法增加占位节点插入逻辑**

修改 `syncReviewItems` 方法，在 "先删除该项目下已有评审项" 之后、"按 level 排序" 之前，新增占位节点插入逻辑：

```java
// 读取评审项配置，为 generateStandard=false 的启用类型插入占位节点
String reviewConfigJson = task.getExtParams(); // 从任务扩展参数获取
if (!StringUtils.hasText(reviewConfigJson)) {
    // 兜底：从项目模板获取
    TbProjectTemplate pt = projectTemplateMapper.selectByProjectId(projectId);
    if (pt != null) {
        reviewConfigJson = pt.getReviewConfig();
    }
}
if (StringUtils.hasText(reviewConfigJson)) {
    ReviewConfig config = ReviewConfig.fromJson(reviewConfigJson);
    for (ReviewTypeConfig typeConfig : config.getEnabledTypes()) {
        if (!typeConfig.isGenerateStandard()) {
            TbProjectReviewItem placeholder = new TbProjectReviewItem();
            placeholder.setProjectId(projectId);
            placeholder.setItemName("详见评审文件");
            placeholder.setLevel(1);
            placeholder.setReviewType(typeConfig.getReviewType());
            placeholder.setSortOrder(items.size());
            items.add(placeholder);
        }
    }
}
```

需在类中注入（如未注入）：

```java
@Autowired
private ProjectTemplateMapper projectTemplateMapper;
```

需新增 import：

```java
import com.jy.eleaitender.common.dto.ReviewConfig;
import com.jy.eleaitender.common.dto.ReviewTypeConfig;
```

- [ ] **Step 2: submitGenerate 时将 reviewConfig 存入任务扩展参数**

修改 `ReviewItemServiceImpl.submitGenerate` 方法，在创建任务前将 reviewConfig 存入 extParams：

```java
// 在 params.put("reviewConfig", ...) 之后
String reviewConfigJson = (String) params.get("reviewConfig");

// 创建任务时传递 extParams
return aiTaskService.createTask(AiTaskType.REVIEW_ITEM_GENERATE,
        projectId, projectId, "PROJECT", params, reviewConfigJson);
```

需确认 `aiTaskService.createTask` 是否支持 extParams 参数。如不支持，改为在任务创建后设置：

```java
AiTask task = aiTaskService.createTask(AiTaskType.REVIEW_ITEM_GENERATE,
        projectId, projectId, "PROJECT", params, null);
if (StringUtils.hasText(reviewConfigJson)) {
    task.setExtParams(reviewConfigJson);
    aiTaskService.updateById(task);
}
return task;
```

- [ ] **Step 3: 验证编译通过**

Run: `cd D:/Archives/IDEAProjects/tender-document-tool/ele-ai-tender-system && rtk mvn compile -pl ele-ai-tender-core -am -Dmaven.test.skip=true`
Expected: BUILD SUCCESS

- [ ] **Step 4: Commit**

```bash
rtk git add ele-ai-tender-system/ele-ai-tender-core/src/main/java/com/jy/eleaitender/core/scheduler/AiTaskResultSyncHandler.java ele-ai-tender-system/ele-ai-tender-core/src/main/java/com/jy/eleaitender/core/service/impl/ReviewItemServiceImpl.java
rtk git commit -m "feat(review): AI结果同步时为generateStandard=false的类型插入占位节点"
```

---

## Task 6: 文档集成 — 根据 reviewConfig 动态输出表格

**Files:**
- Modify: `ele-ai-tender-system/ele-ai-tender-core/src/main/java/com/jy/eleaitender/core/engine/DocumentDataAssembler.java`

- [ ] **Step 1: assemble 方法增加 reviewConfig 读取和动态表格输出**

修改 `DocumentDataAssembler.java` 的 `assemble` 方法，替换评审项表格组装部分。

在类中注入 `ProjectTemplateMapper`：

```java
@Autowired
private ProjectTemplateMapper projectTemplateMapper;
```

替换评审项表格组装部分（从 "评审项（按类型分组）" 注释开始），改为：

```java
// 评审项（按类型分组）
List<TbProjectReviewItem> reviewItems = reviewItemMapper.selectByProjectId(projectId);
Map<String, List<TbProjectReviewItem>> grouped = reviewItems.stream()
        .collect(Collectors.groupingBy(
                item -> item.getReviewType() != null ? item.getReviewType() : "COMPLIANCE",
                LinkedHashMap::new,
                Collectors.toList()
        ));

// 读取评审项配置
ReviewConfig reviewConfig = getReviewConfig(projectId);

// 各类型评审项表格 — 只输出启用的类型
if (reviewConfig == null || reviewConfig.isEnabled("COMPLIANCE")) {
    fillDataList.add(FillData.table("complianceItems",
            buildReviewItemTable(grouped.getOrDefault("COMPLIANCE", Collections.emptyList())), "符合性审查项表格"));
}
if (reviewConfig == null || reviewConfig.isEnabled("TECHNICAL")) {
    fillDataList.add(FillData.table("technicalItems",
            buildReviewItemTable(grouped.getOrDefault("TECHNICAL", Collections.emptyList())), "技术标评审项表格"));
}
if (reviewConfig == null || reviewConfig.isEnabled("CREDIT")) {
    fillDataList.add(FillData.table("creditItems",
            buildReviewItemTable(grouped.getOrDefault("CREDIT", Collections.emptyList())), "资信标评审项表格"));
}
if (reviewConfig == null || reviewConfig.isEnabled("COMMERCIAL")) {
    fillDataList.add(FillData.table("commercialItems",
            buildReviewItemTable(grouped.getOrDefault("COMMERCIAL", Collections.emptyList())), "商务评审项表格"));
}

// 评审项汇总表格 — 只汇总启用的类型
fillDataList.add(FillData.table("allReviewItems",
        buildReviewSummaryTable(grouped, reviewConfig), "评审项汇总表格"));
```

- [ ] **Step 2: 新增 getReviewConfig 辅助方法**

```java
private ReviewConfig getReviewConfig(Long projectId) {
    TbProjectTemplate pt = projectTemplateMapper.selectByProjectId(projectId);
    if (pt != null && pt.getReviewConfig() != null) {
        return ReviewConfig.fromJson(pt.getReviewConfig());
    }
    return null;
}
```

需新增 import：

```java
import com.jy.eleaitender.common.dto.ReviewConfig;
```

- [ ] **Step 3: 修改 buildReviewSummaryTable 支持按配置过滤**

修改 `buildReviewSummaryTable` 方法签名，增加 `ReviewConfig` 参数：

```java
private TableData buildReviewSummaryTable(Map<String, List<TbProjectReviewItem>> grouped,
                                           ReviewConfig reviewConfig) {
```

修改 `toReviewSummaryList` 调用，传入 reviewConfig：

```java
tableData.setRows(toReviewSummaryList(grouped, reviewConfig));
```

修改 `toReviewSummaryList` 方法签名和过滤逻辑：

```java
private List<Map<String, String>> toReviewSummaryList(Map<String, List<TbProjectReviewItem>> grouped,
                                                       ReviewConfig reviewConfig) {
    List<Map<String, String>> result = new ArrayList<>();

    List<String> orderedTypes = List.of("CREDIT", "TECHNICAL", "COMMERCIAL");
    Map<String, String> typeLabels = Map.of(
            "CREDIT", "资信标", "TECHNICAL", "技术标", "COMMERCIAL", "商务标");

    for (String reviewType : orderedTypes) {
        // 跳过未启用的类型
        if (reviewConfig != null && !reviewConfig.isEnabled(reviewType)) {
            continue;
        }

        List<TbProjectReviewItem> items = grouped.getOrDefault(reviewType, Collections.emptyList());
        if (items.isEmpty()) continue;

        // ... 后续逻辑不变 ...
    }
    return result;
}
```

- [ ] **Step 4: 验证编译通过**

Run: `cd D:/Archives/IDEAProjects/tender-document-tool/ele-ai-tender-system && rtk mvn compile -pl ele-ai-tender-core -am -Dmaven.test.skip=true`
Expected: BUILD SUCCESS

- [ ] **Step 5: Commit**

```bash
rtk git add ele-ai-tender-system/ele-ai-tender-core/src/main/java/com/jy/eleaitender/core/engine/DocumentDataAssembler.java
rtk git commit -m "feat(review): 文档集成根据reviewConfig动态输出评审项表格"
```

---

## Task 7: 支撑中心前端 — 模板编辑页评审项配置

**Files:**
- Modify: `ele-ai-tender-support-frontend/src/types/template.ts`
- Modify: `ele-ai-tender-support-frontend/src/views/template/TemplateList.vue`

- [ ] **Step 1: template.ts 类型定义新增 reviewConfig**

在 `TemplateInfo` 中新增：

```typescript
reviewConfig?: ReviewConfig
```

在 `TemplateCreateParams` 和 `TemplateUpdateParams` 中新增：

```typescript
reviewConfig?: string
```

新增类型定义：

```typescript
/** 评审类型配置项 */
export interface ReviewTypeConfig {
  reviewType: string
  enabled: boolean
  generateStandard: boolean
}

/** 评审项配置 */
export interface ReviewConfig {
  reviewTypes: ReviewTypeConfig[]
}

/** 评审类型中文标签映射 */
export const REVIEW_TYPE_LABELS: Record<string, string> = {
  COMPLIANCE: '符合性审查',
  TECHNICAL: '技术标评审',
  CREDIT: '资信标评审',
  COMMERCIAL: '商务评审',
}

/** 构建默认评审配置 */
export function buildDefaultReviewConfig(): ReviewConfig {
  return {
    reviewTypes: Object.keys(REVIEW_TYPE_LABELS).map(type => ({
      reviewType: type,
      enabled: true,
      generateStandard: true,
    })),
  }
}
```

- [ ] **Step 2: TemplateList.vue 新增评审项配置区域**

在模板编辑弹窗中，Word文件上传之后新增评审项配置区域。需要：

1. 在编辑表单数据中新增 `reviewConfig` 响应式变量
2. 打开新增/编辑弹窗时，初始化 `reviewConfig`（新增用默认值，编辑用已有值）
3. 在表单中新增评审项配置表格（el-table 或手动渲染）
4. 保存时序列化 `reviewConfig` 为 JSON 字符串

模板新增关键代码：

```vue
<!-- 评审项配置 -->
<el-divider content-position="left">评审项配置</el-divider>
<el-table :data="formData.reviewConfig.reviewTypes" border size="small">
  <el-table-column label="评审类型" prop="reviewType" width="150">
    <template #default="{ row }">
      {{ REVIEW_TYPE_LABELS[row.reviewType] || row.reviewType }}
    </template>
  </el-table-column>
  <el-table-column label="启用" width="80" align="center">
    <template #default="{ row }">
      <el-switch v-model="row.enabled" size="small" />
    </template>
  </el-table-column>
  <el-table-column label="生成评审标准" width="120" align="center">
    <template #default="{ row }">
      <el-switch v-model="row.generateStandard" size="small" :disabled="!row.enabled" />
    </template>
  </el-table-column>
  <el-table-column label="说明">
    <template #default="{ row }">
      <span v-if="!row.enabled" class="text-gray-400">该类型不参与评审</span>
      <span v-else-if="!row.generateStandard" class="text-orange-400">item_name 将填充"详见评审文件"</span>
      <span v-else class="text-gray-400">AI 生成详细评审项内容</span>
    </template>
  </el-table-column>
</el-table>
```

- [ ] **Step 3: 验证支撑中心前端编译**

Run: `cd D:/Archives/IDEAProjects/tender-document-tool/ele-ai-tender-support-frontend && rtk npm run build`
Expected: 编译成功

- [ ] **Step 4: Commit**

```bash
rtk git add ele-ai-tender-support-frontend/src/types/template.ts ele-ai-tender-support-frontend/src/views/template/TemplateList.vue
rtk git commit -m "feat(review): 支撑中心模板编辑页新增评审项配置区域"
```

---

## Task 8: AI编制前端 — 评审项阶段动态 Tab

**Files:**
- Modify: `ele-ai-tender-frontend/src/types/review.ts`
- Modify: `ele-ai-tender-frontend/src/views/project/phases/PhaseReviewItem.vue`

- [ ] **Step 1: review.ts 类型定义新增 ReviewConfig**

```typescript
/** 评审类型配置项 */
export interface ReviewTypeConfig {
  reviewType: string
  enabled: boolean
  generateStandard: boolean
}

/** 评审项配置 */
export interface ReviewConfig {
  reviewTypes: ReviewTypeConfig[]
}

/** 评审类型中文标签映射 */
export const REVIEW_TYPE_LABELS: Record<string, string> = {
  COMPLIANCE: '符合性审查',
  TECHNICAL: '技术标评审',
  CREDIT: '资信标评审',
  COMMERCIAL: '商务评审',
}
```

- [ ] **Step 2: PhaseReviewItem.vue 动态 Tab**

修改要点：

1. 新增 `reviewConfig` 响应式变量，从项目模板数据中获取
2. 将固定4个 `<el-tab-pane>` 改为 `v-for` 动态渲染 `enabled=true` 的类型
3. `generateStandard=false` 的 Tab 中：隐藏"添加评审项"按钮，显示占位只读行
4. 评分摘要栏只显示启用的类型分值
5. AI 生成时传递 `reviewConfig`

核心改动（伪代码）：

```vue
<!-- 动态Tab -->
<el-tabs v-model="activeTab">
  <el-tab-pane
    v-for="typeConfig in enabledTypes"
    :key="typeConfig.reviewType"
    :label="REVIEW_TYPE_LABELS[typeConfig.reviewType]"
    :name="typeConfig.reviewType"
  >
    <!-- generateStandard=false: 占位只读行 -->
    <template v-if="!typeConfig.generateStandard">
      <el-table :data="placeholderData" border>
        <el-table-column prop="itemName" label="评审项名称" />
        <el-table-column prop="score" label="分值" />
        <!-- 其他列按类型差异化 -->
      </el-table>
      <div class="text-gray-400 mt-2">未生成评审标准，不可手动添加</div>
    </template>
    <!-- generateStandard=true: 正常评审项表格 -->
    <template v-else>
      <!-- 保留原有的树形表格、添加按钮等 -->
    </template>
  </el-tab-pane>
</el-tabs>
```

新增 computed：

```typescript
const enabledTypes = computed(() => {
  if (!reviewConfig.value) {
    // 兜底：无配置时全部启用
    return Object.keys(REVIEW_TYPE_LABELS).map(type => ({
      reviewType: type,
      enabled: true,
      generateStandard: true,
    }))
  }
  return reviewConfig.value.reviewTypes.filter(t => t.enabled)
})

const placeholderData = computed(() => {
  const current = enabledTypes.value.find(t => t.reviewType === activeTab.value)
  if (!current || current.generateStandard) return []
  // 从评审项数据中找到该类型的占位节点
  return reviewItems.value
    .filter(item => item.reviewType === current.reviewType && item.level === 1)
    .map(item => ({
      itemName: item.itemName,
      score: item.score,
    }))
})
```

- [ ] **Step 3: 获取项目模板的 reviewConfig**

在组件初始化时（如 `onMounted`），调用项目模板 API 获取 `reviewConfig`：

```typescript
import { projectTemplateApi } from '@/api/project'

const reviewConfig = ref<ReviewConfig | null>(null)

onMounted(async () => {
  // ... 现有初始化逻辑 ...
  try {
    const res = await projectTemplateApi.getByProject(projectId)
    if (res.data?.reviewConfig) {
      reviewConfig.value = typeof res.data.reviewConfig === 'string'
        ? JSON.parse(res.data.reviewConfig)
        : res.data.reviewConfig
    }
  } catch {
    // 无模板配置，使用默认
  }
})
```

- [ ] **Step 4: 验证AI编制前端编译**

Run: `cd D:/Archives/IDEAProjects/tender-document-tool/ele-ai-tender-frontend && rtk npm run build`
Expected: 编译成功

- [ ] **Step 5: Commit**

```bash
rtk git add ele-ai-tender-frontend/src/types/review.ts ele-ai-tender-frontend/src/views/project/phases/PhaseReviewItem.vue
rtk git commit -m "feat(review): 评审项阶段动态Tab，根据模板reviewConfig展示"
```

---

## 自检清单

| 规范要求 | 对应Task |
|---------|----------|
| sup_template 新增 review_config 字段 | Task 1, 2 |
| tb_project_template 新增 review_config 字段（快照） | Task 1, 2 |
| 项目模板快照复制 reviewConfig | Task 3 |
| AI Prompt 根据 reviewConfig 只生成启用的+需标准的类型 | Task 4 |
| AI 结果同步时为 generateStandard=false 插入占位节点 | Task 5 |
| 文档集成只输出启用类型的表格 | Task 6 |
| 支撑中心模板编辑页评审项配置 UI | Task 7 |
| AI编制前端动态 Tab + 占位展示 | Task 8 |
