# 后缀注册 Redis 一致性修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复后缀可配置化中 DB-Redis 不一致和缓存锁定问题

**Architecture:** ExternalSystemServiceImpl 加事务 + after-commit Redis 刷新 + 原子替换；AllowedTypesResolver 修复错误缓存推进逻辑

**Tech Stack:** Spring @Transactional、TransactionSynchronizationManager、StringRedisTemplate

---

### Task 1: AllowedTypesResolver 缓存 TTL 修复

**Files:**
- Modify: `ele-tender-system/ele-tender-file/src/main/java/com/jy/eletender/file/config/AllowedTypesResolver.java`
- Modify: `ele-tender-system/ele-tender-file/src/test/java/com/jy/eletender/file/config/AllowedTypesResolverTest.java`

- [ ] **Step 1: 写失败场景测试 — Redis 异常后不应锁定缓存**

在 `AllowedTypesResolverTest.java` 末尾新增：

```java
@Test
void isAllowed_shouldRetryAfterRedisFailureInsteadOfLockingCache() {
    when(redisTemplate.opsForSet()).thenReturn(setOperations);
    // 第一次调用 Redis 抛异常
    when(setOperations.members(RedisKeyConstant.REGISTERED_SUFFIXES))
            .thenThrow(new RuntimeException("Redis down"))
            .thenReturn(Set.of(".NewSuffix"));

    // 第一次：Redis 失败，返回空缓存
    assertThat(resolver.isAllowed("file.NewSuffix")).isFalse();

    // 第二次：应该重试 Redis 而不是返回锁定的空缓存
    assertThat(resolver.isAllowed("file.NewSuffix")).isTrue();

    // 验证 Redis 被调用了 2 次（不是 1 次）
    verify(setOperations, times(2)).members(RedisKeyConstant.REGISTERED_SUFFIXES);
}
```

- [ ] **Step 2: 运行测试确认失败**

运行: `mvn -pl ele-tender-file -am test -Dtest=AllowedTypesResolverTest#isAllowed_shouldRetryAfterRedisFailureInsteadOfLockingCache -Dsurefire.failIfNoSpecifiedTests=false`
预期: FAIL — 当前实现 Redis 失败后推进 lastRefreshTime，5 分钟内不会重试

- [ ] **Step 3: 修改 AllowedTypesResolver 实现**

将 `AllowedTypesResolver.java` 的字段和 `getDynamicSuffixes()` 方法改为：

```java
private volatile Set<String> cachedDynamicSuffixes = Collections.emptySet();
private volatile long lastRefreshTime = 0;
private volatile long lastFailTime = 0;

private static final long CACHE_TTL_MS = 5 * 60 * 1000L;
private static final long RETRY_COOLDOWN_MS = 1000L;

private Set<String> getDynamicSuffixes() {
    long now = System.currentTimeMillis();
    if (now - lastRefreshTime < CACHE_TTL_MS) {
        return cachedDynamicSuffixes;
    }
    if (now - lastFailTime < RETRY_COOLDOWN_MS) {
        return cachedDynamicSuffixes;
    }
    try {
        Set<String> members = redisTemplate.opsForSet()
                .members(RedisKeyConstant.REGISTERED_SUFFIXES);
        cachedDynamicSuffixes = (members != null) ? members : Collections.emptySet();
        lastRefreshTime = now;
        log.debug("刷新动态后缀缓存: {}", cachedDynamicSuffixes);
    } catch (Exception e) {
        log.warn("从 Redis 读取注册后缀失败，保留本地缓存", e);
        lastFailTime = now;
    }
    return cachedDynamicSuffixes;
}
```

- [ ] **Step 4: 运行测试确认通过**

运行: `mvn -pl ele-tender-file -am test -Dtest=AllowedTypesResolverTest -Dsurefire.failIfNoSpecifiedTests=false`
预期: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add ele-tender-system/ele-tender-file/src/main/java/com/jy/eletender/file/config/AllowedTypesResolver.java \
       ele-tender-system/ele-tender-file/src/test/java/com/jy/eletender/file/config/AllowedTypesResolverTest.java
git commit -m "fix(file): Redis 故障时不锁定动态后缀缓存"
```

---

### Task 2: ExternalSystemServiceImpl 事务 + 原子 Redis 刷新

**Files:**
- Modify: `ele-tender-system/ele-tender-support/src/main/java/com/jy/eletender/support/service/impl/ExternalSystemServiceImpl.java`
- Modify: `ele-tender-system/ele-tender-support/src/test/java/com/jy/eletender/support/service/ExternalSystemServiceImplSuffixTest.java`

- [ ] **Step 1: 写测试 — Redis 失败时 DB 操作不应回滚**

在 `ExternalSystemServiceImplSuffixTest.java` 末尾新增：

```java
@Test
void createSystem_shouldSucceedEvenWhenRedisRefreshFails() {
    SysAccessSystem system = new SysAccessSystem();
    system.setSystemName("test");
    system.setTenderDocumentSuffix(".ValidZbs");

    when(accessSystemMapper.selectList(any())).thenReturn(List.of(system));
    // 模拟 Redis 异常 — rename 操作失败
    when(redisTemplate.opsForSet()).thenReturn(setOperations);
    doThrow(new RuntimeException("Redis down")).when(redisTemplate).rename(any(), any());

    // 不应抛异常（after-commit 中 Redis 失败被 catch）
    // 注意：单元测试中没有真实事务，afterCommit 回调不会被触发
    // 这里只验证 DB 操作本身成功
    service.createSystem(system);
    verify(accessSystemMapper).insert(system);
}
```

- [ ] **Step 2: 修改 ExternalSystemServiceImpl 实现**

修改 imports 和三个方法 + refreshRegisteredSuffixes：

```java
import org.springframework.transaction.annotation.Transactional;
import org.springframework.transaction.support.TransactionSynchronization;
import org.springframework.transaction.support.TransactionSynchronizationManager;
```

createSystem 加事务 + after-commit：
```java
@Override
@Transactional(rollbackFor = Exception.class)
public SysAccessSystem createSystem(SysAccessSystem system) {
    validateSuffixes(system);
    system.setAppKey(generateAppKey());
    system.setAppSecret(generateAppSecret());
    system.setStatus(1);
    accessSystemMapper.insert(system);
    registerAfterCommitSuffixRefresh();
    return system;
}
```

updateSystem 同理：
```java
@Override
@Transactional(rollbackFor = Exception.class)
public void updateSystem(SysAccessSystem system) {
    validateSuffixes(system);
    system.setAppKey(null);
    system.setAppSecret(null);
    accessSystemMapper.updateById(system);
    registerAfterCommitSuffixRefresh();
}
```

deleteSystem 同理：
```java
@Override
@Transactional(rollbackFor = Exception.class)
public void deleteSystem(Long id) {
    accessSystemMapper.deleteById(id);
    registerAfterCommitSuffixRefresh();
}
```

新增 after-commit 注册方法：
```java
private void registerAfterCommitSuffixRefresh() {
    if (!TransactionSynchronizationManager.isSynchronizationActive()) {
        // 无事务上下文（如单元测试），直接执行
        safeRefreshRegisteredSuffixes();
        return;
    }
    TransactionSynchronizationManager.registerSynchronization(
            new TransactionSynchronization() {
                @Override
                public void afterCommit() {
                    safeRefreshRegisteredSuffixes();
                }
            });
}

private void safeRefreshRegisteredSuffixes() {
    try {
        refreshRegisteredSuffixes();
    } catch (Exception e) {
        log.error("DB 已提交但 Redis 后缀刷新失败，将在下次操作时自愈", e);
    }
}
```

refreshRegisteredSuffixes 改为原子替换：
```java
private void refreshRegisteredSuffixes() {
    LambdaQueryWrapper<SysAccessSystem> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(SysAccessSystem::getIsDelete, 0);
    wrapper.and(w -> w.isNotNull(SysAccessSystem::getTenderDocumentSuffix)
            .or()
            .isNotNull(SysAccessSystem::getBidDocumentSuffix));
    List<SysAccessSystem> systems = accessSystemMapper.selectList(wrapper);

    Set<String> suffixes = new LinkedHashSet<>();
    for (SysAccessSystem sys : systems) {
        if (StringUtils.hasText(sys.getTenderDocumentSuffix())) {
            suffixes.add(sys.getTenderDocumentSuffix());
        }
        if (StringUtils.hasText(sys.getBidDocumentSuffix())) {
            suffixes.add(sys.getBidDocumentSuffix());
        }
    }

    String key = RedisKeyConstant.REGISTERED_SUFFIXES;
    if (suffixes.isEmpty()) {
        redisTemplate.delete(key);
    } else {
        String tmpKey = key + ":tmp:" + UUID.randomUUID();
        try {
            redisTemplate.opsForSet().add(tmpKey, suffixes.toArray(String[]::new));
            redisTemplate.rename(tmpKey, key);
        } catch (Exception e) {
            redisTemplate.delete(tmpKey);
            throw e;
        }
    }
    log.info("已刷新注册后缀到 Redis: {}", suffixes);
}
```

- [ ] **Step 3: 更新现有测试以适配新的 Redis 操作**

现有测试 `createSystem_shouldAcceptValidSuffixAndSyncRedis` 验证 `delete` + `add`，需要改为验证 `add(tmpKey)` + `rename`：

```java
@Test
void createSystem_shouldAcceptValidSuffixAndSyncRedis() {
    SysAccessSystem system = new SysAccessSystem();
    system.setSystemName("test");
    system.setTenderDocumentSuffix(".CustomZbs");

    SysAccessSystem existing = new SysAccessSystem();
    existing.setTenderDocumentSuffix(".OtherZbs");
    existing.setBidDocumentSuffix(".OtherTbs");
    when(accessSystemMapper.selectList(any())).thenReturn(List.of(existing, system));

    service.createSystem(system);

    verify(accessSystemMapper).insert(system);
    // 验证写入临时 key 并 rename
    @SuppressWarnings("unchecked")
    ArgumentCaptor<String> keyCaptor = ArgumentCaptor.forClass(String.class);
    @SuppressWarnings("unchecked")
    ArgumentCaptor<String[]> valCaptor = ArgumentCaptor.forClass(String[].class);
    verify(setOperations).add(keyCaptor.capture(), valCaptor.capture());
    assertThat(keyCaptor.getValue()).startsWith(RedisKeyConstant.REGISTERED_SUFFIXES + ":tmp:");
    assertThat(valCaptor.getValue()).containsExactlyInAnyOrder(".OtherZbs", ".OtherTbs", ".CustomZbs");
    verify(redisTemplate).rename(keyCaptor.getValue(), RedisKeyConstant.REGISTERED_SUFFIXES);
}
```

- [ ] **Step 4: 运行测试确认通过**

运行: `mvn -pl ele-tender-support -am test -Dtest=ExternalSystemServiceImplSuffixTest -Dsurefire.failIfNoSpecifiedTests=false`
预期: 全部 PASS

- [ ] **Step 5: 提交**

```bash
git add ele-tender-system/ele-tender-support/src/main/java/com/jy/eletender/support/service/impl/ExternalSystemServiceImpl.java \
       ele-tender-system/ele-tender-support/src/test/java/com/jy/eletender/support/service/ExternalSystemServiceImplSuffixTest.java
git commit -m "fix(support): 后缀 Redis 刷新改为事务 after-commit + 原子替换"
```

---

### Task 3: 设计文档提交

**Files:**
- Add: `docs/projects/2026-04-03-后缀注册Redis一致性修复设计.md`

- [ ] **Step 1: 提交设计文档**

```bash
git add "docs/projects/2026-04-03-后缀注册Redis一致性修复设计.md"
git commit -m "docs(projects): 后缀注册 Redis 一致性修复设计"
```

---

## 验证

1. `mvn -pl ele-tender-file -am clean test` — AllowedTypesResolver 缓存行为测试
2. `mvn -pl ele-tender-support -am clean test` — 事务 + Redis 刷新测试
