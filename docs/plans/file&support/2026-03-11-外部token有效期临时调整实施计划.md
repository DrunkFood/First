# 外部 Token 有效期临时调整实施计划

**Goal:** 将外部 token 有效期临时调整为 7 天，并保持 JWT、Redis、接口返回值一致。

**Scope:** `ele-tender-common`、`ele-tender-support`（仅依赖常量的行为联动）。

### Task 1: 先补回归测试
- 新增 `JwtUtil` 外部 token 过期时长测试，先验证当前实现失败（RED）。

### Task 2: 调整有效期常量
- 修改 `JwtUtil` 外部 token 过期毫秒值为 7 天。
- 修改 `RedisKeyConstant.EXTERNAL_TOKEN_EXPIRE_SECONDS` 为 7 天秒数。

### Task 3: 执行最小验证
- 运行 `ele-tender-common` 相关测试，确认新增测试转绿且无回归。
