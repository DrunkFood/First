# 文档预览改造与前端验证 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 DocumentPreview.vue 从 HTML 预览改造为 docx-preview 渲染，提取公共 DocxPreview 组件，并验证前后端全流程

**Architecture:** 从 PhaseDocument.vue 提取 docx-preview 渲染逻辑为独立 DocxPreview.vue 公共组件，DocumentPreview.vue 和 PhaseDocument.vue 均使用该组件渲染 Word 预览。未集成的文档禁用预览按钮。

**Tech Stack:** Vue 3 · TypeScript · docx-preview 0.3.7 · Element Plus · Vite

---

## File Structure

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `src/components/document/DocxPreview.vue` | docx-preview 渲染公共组件 |
| 改造 | `src/components/document/DocumentPreview.vue` | v-html → DocxPreview，未集成禁用预览 |
| 重构 | `src/views/project/phases/PhaseDocument.vue` | 提取渲染逻辑到 DocxPreview |
| 改造 | `src/views/project/ProjectDetail.vue` | 未集成时禁用预览按钮 |

所有文件均在 `ele-ai-tender-frontend/` 目录下。

---

### Task 1: 创建 DocxPreview.vue 公共组件

**Files:**
- Create: `src/components/document/DocxPreview.vue`

- [ ] **Step 1: 创建 DocxPreview.vue 组件文件**

```vue
<template>
  <div class="docx-preview-wrapper">
    <div v-if="loading" class="docx-preview-loading">
      <el-icon class="is-loading" :size="24"><Loading /></el-icon>
      <span>文档加载中...</span>
    </div>
    <div
      v-show="!loading"
      ref="containerRef"
      class="docx-preview-content"
      :style="{ transform: `scale(${zoom / 100})`, transformOrigin: 'top left' }"
    />
  </div>
</template>

<script setup lang="ts">
import { ref, watch, nextTick } from 'vue'
import { ElMessage } from 'element-plus'
import { Loading } from '@element-plus/icons-vue'
import { renderAsync } from 'docx-preview'
import { fileApi } from '@/api/file'

const props = withDefaults(defineProps<{
  fileId: number | null
  zoom?: number
}>(), {
  zoom: 100,
})

const loading = ref(false)
const containerRef = ref<HTMLElement | null>(null)

const renderDocx = async (fileId: number) => {
  loading.value = true
  try {
    const blob = await fileApi.download(fileId) as unknown as Blob
    await nextTick()
    if (containerRef.value) {
      containerRef.value.innerHTML = ''
      await renderAsync(blob, containerRef.value, undefined, {
        className: 'docx-preview-wrapper',
        inWrapper: true,
        ignoreWidth: false,
        ignoreHeight: false,
        ignoreFonts: false,
        breakPages: true,
        ignoreLastRenderedPageBreak: true,
        experimental: false,
        trimXmlDeclaration: true,
        debug: false,
      })
    }
  } catch (e) {
    console.error('Word文档渲染失败:', e)
    ElMessage.warning('Word文档渲染失败，请使用下载功能查看')
  } finally {
    loading.value = false
  }
}

watch(() => props.fileId, (newId) => {
  if (newId) {
    renderDocx(newId)
  } else if (containerRef.value) {
    containerRef.value.innerHTML = ''
  }
}, { immediate: true })
</script>

<style scoped>
.docx-preview-wrapper {
  position: relative;
}

.docx-preview-loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 60px 20px;
  gap: 12px;
  color: var(--app-text-secondary);
  font-size: 14px;
}

.docx-preview-content {
  min-height: 500px;
}

.docx-preview-content :deep(.docx-wrapper) {
  background: white;
  padding: 0;
}

.docx-preview-content :deep(.docx-wrapper > section.docx) {
  box-shadow: none;
  margin-bottom: 0;
  padding: 0;
}
</style>
```

- [ ] **Step 2: 验证编译通过**

Run: `cd ele-ai-tender-frontend && npx vue-tsc --noEmit 2>&1 | head -20`
Expected: 无类型错误（DocxPreview 还未被引用，不应有影响）

---

### Task 2: 重构 PhaseDocument.vue 使用 DocxPreview

**Files:**
- Modify: `src/views/project/phases/PhaseDocument.vue`

- [ ] **Step 1: 替换 import 和移除旧代码**

在 `<script setup>` 中：
- 添加 `import DocxPreview from '@/components/document/DocxPreview.vue'`
- 移除 `import { renderAsync } from 'docx-preview'`
- 移除 `import { Loading } from '@element-plus/icons-vue'`（Loading 图标改由 DocxPreview 内部使用，但 PhaseDocument 的加载状态仍保留自己的 Loading 引用——检查是否还有其他地方用到）

具体变更：

```typescript
// 移除
import { renderAsync } from 'docx-preview'

// 添加
import DocxPreview from '@/components/document/DocxPreview.vue'
```

- [ ] **Step 2: 移除旧渲染逻辑**

移除以下变量和函数：
- `const isDocxLoading = ref(false)` — 不再需要，DocxPreview 内部管理
- `const docxPreviewContainer = ref<HTMLElement | null>(null)` — 不再需要
- `renderDocxPreview()` 函数整体移除

移除 `loadPreview()` 中对 `renderDocxPreview` 的调用：
```typescript
// 修改前
const loadPreview = async () => {
  preview.value = await documentApi.getPreview(props.projectId)
  if (preview.value?.markdownContent) {
    markdownContent.value = preview.value.markdownContent
  }
  if (preview.value?.integrated && preview.value?.generatedFileId) {
    await renderDocxPreview(preview.value.generatedFileId)
  }
}

// 修改后
const loadPreview = async () => {
  preview.value = await documentApi.getPreview(props.projectId)
}
```

移除 `handleIntegrate()` 中对 `renderDocxPreview` 的调用：
```typescript
// 修改前
const handleIntegrate = async () => {
  isIntegrating.value = true
  try {
    preview.value = await documentApi.integrate(props.projectId)
    if (preview.value?.markdownContent) {
      markdownContent.value = preview.value.markdownContent
    }
    if (preview.value?.generatedFileId) {
      await renderDocxPreview(preview.value.generatedFileId)
    }
    ElMessage.success('文档集成完成')
  } catch {
    ElMessage.error('文档集成失败')
  } finally {
    isIntegrating.value = false
  }
}

// 修改后
const handleIntegrate = async () => {
  isIntegrating.value = true
  try {
    preview.value = await documentApi.integrate(props.projectId)
    ElMessage.success('文档集成完成')
  } catch {
    ElMessage.error('文档集成失败')
  } finally {
    isIntegrating.value = false
  }
}
```

- [ ] **Step 3: 替换模板中的预览区域**

```html
<!-- 修改前 (行 154-163) -->
<div
  ref="docxPreviewContainer"
  class="preview-content docx-preview-container"
  :style="{ transform: `scale(${zoomLevel / 100})`, transformOrigin: 'top left' }"
/>
<div v-if="isDocxLoading" class="docx-loading">
  <el-icon class="is-loading" :size="24"><Loading /></el-icon>
  <span>文档加载中...</span>
</div>

<!-- 修改后 -->
<DocxPreview :file-id="preview?.generatedFileId ?? null" :zoom="zoomLevel" />
```

- [ ] **Step 4: 移除不再需要的 CSS**

移除 `.docx-preview-container` 的 `:deep()` 样式块（行 782-793），因为样式已内置于 DocxPreview 组件。

移除 `.docx-loading` 样式块（行 795-804），加载状态由 DocxPreview 内部管理。

- [ ] **Step 5: 验证编译通过**

Run: `cd ele-ai-tender-frontend && npx vue-tsc --noEmit 2>&1 | head -20`
Expected: 无类型错误

---

### Task 3: 改造 DocumentPreview.vue 使用 DocxPreview

**Files:**
- Modify: `src/components/document/DocumentPreview.vue`

- [ ] **Step 1: 重写 DocumentPreview.vue**

```vue
<template>
  <el-dialog
    :model-value="modelValue"
    title="文档预览"
    fullscreen
    @update:model-value="$emit('update:modelValue', $event)"
    @open="handleOpen"
  >
    <template #header>
      <div class="preview-header">
        <span class="preview-title">文档预览</span>
        <div class="preview-toolbar">
          <el-button :icon="Download" @click="handleDownload" :loading="downloadLoading">下载</el-button>
          <el-button :icon="Printer" @click="handlePrint">打印</el-button>
          <el-button :icon="Close" @click="$emit('update:modelValue', false)">关闭</el-button>
        </div>
      </div>
    </template>

    <div v-loading="loading" class="document-preview">
      <template v-if="integrated && fileId">
        <DocxPreview :file-id="fileId" />
      </template>
      <el-empty v-else-if="!loading" description="暂无文档内容" />
    </div>
  </el-dialog>
</template>

<script setup lang="ts">
import { ref } from 'vue'
import { ElMessage } from 'element-plus'
import { Download, Printer, Close } from '@element-plus/icons-vue'
import { documentApi } from '@/api/document'
import { fileApi } from '@/api/file'
import DocxPreview from '@/components/document/DocxPreview.vue'

const props = defineProps<{
  modelValue: boolean
  projectId: number
  generatedFileId?: number | null
}>()

defineEmits<{
  'update:modelValue': [value: boolean]
}>()

const loading = ref(false)
const downloadLoading = ref(false)
const fileId = ref<number | null>(null)
const integrated = ref(false)
const projectName = ref('文档')

async function handleOpen() {
  if (!props.projectId) return
  loading.value = true
  try {
    const data = await documentApi.getPreview(props.projectId)
    fileId.value = data.generatedFileId
    integrated.value = data.integrated
    projectName.value = data.projectName || '文档'
  } catch {
    fileId.value = null
    integrated.value = false
    ElMessage.error('获取文档预览失败')
  } finally {
    loading.value = false
  }
}

async function handleDownload() {
  const targetFileId = fileId.value || props.generatedFileId
  if (!targetFileId) {
    ElMessage.warning('文档尚未生成，无法下载')
    return
  }
  downloadLoading.value = true
  try {
    const blob = await fileApi.download(targetFileId)
    const url = window.URL.createObjectURL(blob as Blob)
    const link = document.createElement('a')
    link.href = url
    link.download = `${projectName.value}.docx`
    link.click()
    window.URL.revokeObjectURL(url)
    ElMessage.success('文档下载成功')
  } catch {
    ElMessage.error('文档下载失败')
  } finally {
    downloadLoading.value = false
  }
}

function handlePrint() {
  window.print()
}
</script>

<style scoped>
.preview-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  width: 100%;
}
.preview-title {
  font-size: 18px;
  font-weight: 600;
}
.preview-toolbar {
  display: flex;
  gap: 8px;
}
.document-preview {
  min-height: 400px;
}
</style>
```

- [ ] **Step 2: 验证编译通过**

Run: `cd ele-ai-tender-frontend && npx vue-tsc --noEmit 2>&1 | head -20`
Expected: 无类型错误

---

### Task 4: 改造 ProjectDetail.vue 未集成时禁用预览

**Files:**
- Modify: `src/views/project/ProjectDetail.vue`

- [ ] **Step 1: 禁用预览按钮**

将项目详情页的两个"预览"按钮添加禁用条件。当 `project.generatedFileId` 为空时禁用：

```html
<!-- 修改前 (行 74) -->
<el-button type="primary" :icon="View" @click="showPreview = true">预览文档</el-button>

<!-- 修改后 -->
<el-button type="primary" :icon="View" :disabled="!project?.generatedFileId" @click="showPreview = true">预览文档</el-button>

<!-- 修改前 (行 119) -->
<el-button type="primary" :icon="View" @click="showPreview = true">预览</el-button>

<!-- 修改后 -->
<el-button type="primary" :icon="View" :disabled="!project?.generatedFileId" @click="showPreview = true">预览</el-button>
```

- [ ] **Step 2: 验证编译通过**

Run: `cd ele-ai-tender-frontend && npx vue-tsc --noEmit 2>&1 | head -20`
Expected: 无类型错误

---

### Task 5: 前端构建验证

- [ ] **Step 1: 运行前端类型检查**

Run: `cd ele-ai-tender-frontend && npx vue-tsc --noEmit`
Expected: 无错误

- [ ] **Step 2: 运行前端构建**

Run: `cd ele-ai-tender-frontend && npm run build`
Expected: 构建成功

- [ ] **Step 3: 提交代码**

```bash
cd ele-ai-tender-frontend
git add src/components/document/DocxPreview.vue
git add src/components/document/DocumentPreview.vue
git add src/views/project/phases/PhaseDocument.vue
git add src/views/project/ProjectDetail.vue
git commit -m "feat(frontend): 提取DocxPreview公共组件，改造文档预览为Word渲染"
```

---

### Task 6: 启动后端服务

- [ ] **Step 1: 安装 common 模块**

Run: `cd ele-ai-tender-system && mvn install -pl ele-ai-tender-common -am -Dmaven.test.skip=true`
Expected: BUILD SUCCESS

- [ ] **Step 2: 启动 File 服务**

Run: `cd ele-ai-tender-system/ele-ai-tender-file && mvn spring-boot:run`
Expected: 服务启动在 :8081

- [ ] **Step 3: 启动 Support 服务**

Run: `cd ele-ai-tender-system/ele-ai-tender-support && mvn spring-boot:run`
Expected: 服务启动在 :8080

- [ ] **Step 4: 启动 Core 服务**

Run: `cd ele-ai-tender-system/ele-ai-tender-core && mvn spring-boot:run`
Expected: 服务启动在 :8082

---

### Task 7: 支撑前端上传验证

- [ ] **Step 1: 启动支撑前端**

Run: `cd ele-ai-tender-support-frontend && npm run dev`
Expected: 服务启动在 :3060

- [ ] **Step 2: 在浏览器中验证完整上传流程**

打开 http://localhost:3060，按以下步骤验证：

1. 登录支撑中心（admin / 123456）
2. 进入模板管理页面，确认列表正常加载
3. 点击新建模板，确认表单含文件上传控件
4. 填写模板名称、项目类别、项目类型，上传 .docx 文件
5. 提交创建，确认 structureDefinition 自动填充（章节数+占位符数）
6. 编辑已有模板，替换 Word 文件，确认 structureDefinition 更新
7. 设为默认模板，确认该类别+类型下只有一个默认
8. 删除模板，确认逻辑删除成功

- [ ] **Step 3: 记录发现的问题**

如发现 bug，记录到本计划的评论区，修复后补充 commit。

---

### Task 8: 业务前端模板绑定验证

- [ ] **Step 1: 启动业务前端**

Run: `cd ele-ai-tender-frontend && npm run dev`
Expected: 服务启动在 :5173

- [ ] **Step 2: 在浏览器中验证完整绑定流程**

打开 http://localhost:5173，按以下步骤验证：

1. 登录 AI 编制前端（admin / 123456）
2. 创建新项目，进入 PhaseBasicInfo
3. 选择项目类别+类型，确认自动选择默认模板
4. 手动切换模板，确认卡片选中状态更新
5. 保存并下一步，确认 `projectTemplateApi.bind()` 成功
6. 进入文档阶段，执行文档集成，确认生成 Word 文件
7. 点击预览，确认 DocxPreview 渲染成功
8. 返回项目详情页，确认预览按钮已集成时可用、未集成时禁用

- [ ] **Step 3: 记录发现的问题**

如发现 bug，记录到本计划的评论区，修复后补充 commit。

---

### Task 9: 最终提交与清理

- [ ] **Step 1: 确认所有文件变更正确**

Run: `cd ele-ai-tender-frontend && rtk git status && rtk git diff`
Expected: 只有 DocxPreview.vue(新增)、DocumentPreview.vue(修改)、PhaseDocument.vue(修改)、ProjectDetail.vue(修改) 的变更

- [ ] **Step 2: 最终构建验证**

Run: `cd ele-ai-tender-frontend && npm run build`
Expected: 构建成功
